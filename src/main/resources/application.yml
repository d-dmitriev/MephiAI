management:
  endpoints:
    web:
      exposure:
        include: prometheus
  metrics:
    tags:
      application: spring-boot-sentiment
server:
  tomcat:
    mbeanregistry:
      enabled: true
app:
  model:
    path: models/final_sentiment_model.proto